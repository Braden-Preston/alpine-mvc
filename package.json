{
  "name": "alpine-mvc",
  "version": "1.0.0",
  "main": "index.js",
  "license": "GPL-2.0-only",
  "scripts": {
    "dev": "run-p 'dev:*'",
    "dev:server": "fastify start -w -l info -P build",
    "dev:bundle": "esbuild app/index.js --bundle --platform=node --watch --outdir=build",
    "start": "fastify start -l info app"
  },
  "dependencies": {
    "fastify": "^3.22.0",
    "fastify-cors": "^6.0.2",
    "fastify-static": "^4.4.2",
    "middie": "^5.3.0"
  },
  "devDependencies": {
    "esbuild": "^0.13.7",
    "fastify-cli": "^2.13.0",
    "npm-run-all": "^4.1.5"
  }
}
